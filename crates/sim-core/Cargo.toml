[package]
name = "sim-core"
description = "Deterministic, platform-independent diesel truck engine simulation core."
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
publish.workspace = true

# sim-core must not depend on browser, DOM, audio-device, or filesystem APIs.
# The only dependencies are data (de)serialization.
[dependencies]
serde = { workspace = true }
serde_json = { workspace = true }

[lints.rust]
unsafe_code = "forbid"
